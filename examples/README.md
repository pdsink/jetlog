# Examples

## [native](./native)

All configuration and usage examples you may need, read first.

```sh
pio run -e native -t exec
```

## [arduino_esp32-c3](./arduino_esp32-c3)

How to wire the logger into real hardware (via low priority task).

```sh
pio run -e example_arduino_esp32-c3
```
